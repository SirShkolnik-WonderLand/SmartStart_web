# AliceSolutionsGroup Website - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git

# Server Configuration
NODE_ENV=development
PORT=3346

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@alicesolutionsgroup.com
SMTP_PASS=your-app-specific-password

# Security
ADMIN_TOKEN=generate-a-secure-random-token-here
SESSION_SECRET=generate-another-secure-random-secret

# Analytics & SEO
GOOGLE_API_KEY=your-google-api-key-for-seo-monitoring

# Generate secure tokens with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
