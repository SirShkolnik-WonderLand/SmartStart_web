services:
  - type: web
    name: smartstart-website
    env: node
    plan: starter
    buildCommand: cd smartstart-platform && npm ci --production --cache .npm-cache --prefer-offline
    startCommand: cd smartstart-platform && npm start
    envVars:
      - key: NODE_ENV
        value: production